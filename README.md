# Rotating cube in OpenGL

## Installation

Dependencies:

- GLFW: glfw-devel / libglfw, depend on your package manager
- GLAD (in `glad/`): OpenGL extension loader
- Zig 0.15.*

then `zig build run`.

